FROM node:22-alpine

WORKDIR /runner
RUN addgroup -S runner && adduser -S runner -G runner
USER runner

CMD ["node", "-e", "console.log('Gencode runner contract container. Implement language adapters here.')"]
